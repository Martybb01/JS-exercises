const person = {
  id: 1,
  firstName: "Mario",
  lastName: "Rossi",
  age: 25,
};

// i valori vengono assegnati per naming
let { id, firstName, lastName, age } = person;

console.log(id, firstName, lastName, age);
